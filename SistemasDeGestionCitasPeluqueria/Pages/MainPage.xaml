<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:SistemasDeGestionCitasPeluqueria.PageModels"
             xmlns:m="clr-namespace:SistemasDeGestionCitasPeluqueria.Models"
             xmlns:shapes="clr-namespace:Microsoft.Maui.Controls.Shapes;assembly=Microsoft.Maui.Controls"
             x:Class="SistemasDeGestionCitasPeluqueria.Pages.MainPage"
             x:DataType="vm:MainPageModel"
             Title="Inicio"
             BackgroundColor="{DynamicResource Surface}">

    <ScrollView VerticalScrollBarVisibility="Always"
                HorizontalScrollBarVisibility="Never">
        <VerticalStackLayout Padding="16" Spacing="16">

            <!-- HERO (dinámico) -->
            <Frame Padding="0" HasShadow="True" Style="{StaticResource CardFrameStyle}">
                <Border>
                    <Border.StrokeShape>
                        <shapes:RoundRectangle CornerRadius="12" />
                    </Border.StrokeShape>

                    <Grid HeightRequest="220">
                        <Image Aspect="AspectFill"
                               Source="{Binding HeroImageUrl, FallbackValue=https://picsum.photos/1200/400?blur=2}" />
                        <Grid>
                            <Grid.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="{DynamicResource HeroOverlayTop}" Offset="0.25" />
                                    <GradientStop Color="{DynamicResource HeroOverlayMid}" Offset="0.55" />
                                    <GradientStop Color="{DynamicResource HeroOverlayBottom}" Offset="1.0" />
                                </LinearGradientBrush>
                            </Grid.Background>
                        </Grid>

                        <VerticalStackLayout Padding="16" Spacing="6" VerticalOptions="End" HorizontalOptions="Start">
                            <Label Text="{Binding BarbershopName}"
                                   TextColor="{StaticResource White}"
                                   FontAttributes="Bold"
                                   FontSize="24" />
                            <Label Text="Tu estilo, nuestra pasión"
                                   TextColor="{StaticResource Gray200}"
                                   FontSize="14" />
                        </VerticalStackLayout>
                    </Grid>
                </Border>
            </Frame>

            <!-- Tarjetas de información (dinámicas) -->
            <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto" ColumnSpacing="16" RowSpacing="16">
                <!-- Horario -->
                <Frame Grid.Row="0" Grid.Column="0" Style="{StaticResource CardFrameStyle}" Padding="14">
                    <HorizontalStackLayout Spacing="12" VerticalOptions="Center">
                        <Border BackgroundColor="{DynamicResource ServiceIconBgLight}" Padding="10">
                            <Border.StrokeShape>
                                <shapes:RoundRectangle CornerRadius="10" />
                            </Border.StrokeShape>
                            <Label FontFamily="{StaticResource SymbolThemeFontFamily}"
                                   Text="{StaticResource IconGlyph.Schedule}"
                                   FontSize="18"
                                   TextColor="{DynamicResource ServiceIconAccent}" />
                        </Border>
                        <VerticalStackLayout Spacing="2">
                            <Label Text="Horario" FontAttributes="Bold" />
                            <Label Text="{Binding OpeningText}"
                                   FontSize="12" TextColor="{DynamicResource TextSecondary}" />
                        </VerticalStackLayout>
                    </HorizontalStackLayout>
                </Frame>

                <!-- Ubicación -->
                <Frame Grid.Row="0" Grid.Column="1" Style="{StaticResource CardFrameStyle}" Padding="14">
                    <HorizontalStackLayout Spacing="12" VerticalOptions="Center">
                        <Border BackgroundColor="{DynamicResource ServiceIconBgLight}" Padding="10">
                            <Border.StrokeShape>
                                <shapes:RoundRectangle CornerRadius="10" />
                            </Border.StrokeShape>
                            <Label FontFamily="{StaticResource SymbolThemeFontFamily}"
                                   Text="{StaticResource IconGlyph.Location}"
                                   FontSize="18"
                                   TextColor="{DynamicResource ServiceIconAccent}" />
                        </Border>
                        <VerticalStackLayout Spacing="2">
                            <Label Text="Ubicación" FontAttributes="Bold" />
                            <Label Text="{Binding AddressText}"
                                   FontSize="12" TextColor="{DynamicResource TextSecondary}" />
                        </VerticalStackLayout>
                    </HorizontalStackLayout>
                </Frame>

                <!-- Contacto -->
                <Frame Grid.Row="1" Grid.Column="0" Style="{StaticResource CardFrameStyle}" Padding="14">
                    <HorizontalStackLayout Spacing="12" VerticalOptions="Center">
                        <Border BackgroundColor="{DynamicResource ServiceIconBgLight}" Padding="10">
                            <Border.StrokeShape>
                                <shapes:RoundRectangle CornerRadius="10" />
                            </Border.StrokeShape>
                            <Label FontFamily="{StaticResource SymbolThemeFontFamily}"
                                   Text="{StaticResource IconGlyph.Phone}"
                                   FontSize="18"
                                   TextColor="{DynamicResource ServiceIconAccent}" />
                        </Border>
                        <VerticalStackLayout Spacing="2">
                            <Label Text="Contacto" FontAttributes="Bold" />
                            <Label Text="{Binding ContactText}"
                                   FontSize="12" TextColor="{DynamicResource TextSecondary}" />
                        </VerticalStackLayout>
                    </HorizontalStackLayout>
                </Frame>

                <!-- Reservas (texto fijo por ahora) -->
                <Frame Grid.Row="1" Grid.Column="1" Style="{StaticResource CardFrameStyle}" Padding="14">
                    <HorizontalStackLayout Spacing="12" VerticalOptions="Center">
                        <Border BackgroundColor="{DynamicResource ServiceIconBgLight}" Padding="10">
                            <Border.StrokeShape>
                                <shapes:RoundRectangle CornerRadius="10" />
                            </Border.StrokeShape>
                            <Label FontFamily="{StaticResource SymbolThemeFontFamily}"
                                   Text="{StaticResource IconGlyph.Calendar}"
                                   FontSize="18"
                                   TextColor="{DynamicResource ServiceIconAccent}" />
                        </Border>
                        <VerticalStackLayout Spacing="2">
                            <Label Text="Reservas" FontAttributes="Bold" />
                            <Label Text="Reserva en línea&#10;Fácil y rápido"
                                   FontSize="12" TextColor="{DynamicResource TextSecondary}" />
                        </VerticalStackLayout>
                    </HorizontalStackLayout>
                </Frame>
            </Grid>

            <!-- Nuestros Trabajos (desde /gallery) -->
            <Label Text="Nuestros Trabajos" Style="{StaticResource SectionTitleStyle}" />
            <ScrollView Orientation="Horizontal"
                        HorizontalScrollBarVisibility="Always"
                        VerticalScrollBarVisibility="Never">
                <HorizontalStackLayout Spacing="12"
                                       BindableLayout.ItemsSource="{Binding Gallery}">
                    <BindableLayout.ItemTemplate>
                        <DataTemplate x:DataType="m:GalleryItem">
                            <Frame Style="{StaticResource ImageCardStyle}" WidthRequest="260" HeightRequest="160">
                                <Image Aspect="AspectFill"
                                       Source="{Binding ImageUrl}" />
                            </Frame>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </HorizontalStackLayout>
            </ScrollView>

            <!-- Estado de carga -->
            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />

            <!-- Servicios (sin cambios) -->
            <Label Text="Servicios" Style="{StaticResource SectionTitleStyle}" />
            <ScrollView Orientation="Horizontal"
                        HorizontalScrollBarVisibility="Always"
                        VerticalScrollBarVisibility="Never"
                        HeightRequest="170">
                <HorizontalStackLayout Spacing="12"
                                       BindableLayout.ItemsSource="{Binding Services}">
                    <BindableLayout.ItemTemplate>
                        <DataTemplate x:DataType="m:ServiceOffering">
                            <Frame Padding="12" WidthRequest="230" HasShadow="True" Style="{StaticResource CardFrameStyle}">
                                <VerticalStackLayout Spacing="4">
                                    <Label Text="{Binding Name}" FontAttributes="Bold" />
                                    <Label Text="{Binding Description}"
                                           FontSize="12"
                                           TextColor="{DynamicResource TextSecondary}"
                                           LineBreakMode="TailTruncation" />
                                    <HorizontalStackLayout Spacing="12">
                                        <Label Text="{Binding Price, StringFormat='{}{0:C}'}" TextColor="{DynamicResource Accent}" />
                                        <Label Text="{Binding DurationMinutes, StringFormat='{}{0} min'}" FontSize="12" />
                                    </HorizontalStackLayout>
                                </VerticalStackLayout>
                            </Frame>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </HorizontalStackLayout>
            </ScrollView>

            <!-- Barberos (sin cambios) -->
            <Label Text="Barberos" Style="{StaticResource SectionTitleStyle}" />
            <ScrollView Orientation="Horizontal"
                        HorizontalScrollBarVisibility="Always"
                        VerticalScrollBarVisibility="Never"
                        HeightRequest="190">
                <HorizontalStackLayout Spacing="12"
                                       BindableLayout.ItemsSource="{Binding Barbers}">
                    <BindableLayout.ItemTemplate>
                        <DataTemplate x:DataType="m:Barber">
                            <Frame Padding="10" WidthRequest="180" HasShadow="True" Style="{StaticResource CardFrameStyle}">
                                <VerticalStackLayout Spacing="6">
                                    <Image Source="{Binding PhotoUrl}" HeightRequest="90" Aspect="AspectFill" />
                                    <Label Text="{Binding Name}" FontAttributes="Bold" />
                                    <Label Text="{Binding Specialty}" FontSize="12" TextColor="{DynamicResource TextSecondary}" />
                                </VerticalStackLayout>
                            </Frame>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </HorizontalStackLayout>
            </ScrollView>

            <!-- Productos destacados (sin cambios) -->
            <Label Text="Productos destacados" Style="{StaticResource SectionTitleStyle}" />
            <CollectionView ItemsSource="{Binding FeaturedProducts}" HeightRequest="260">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical" Span="2" />
                </CollectionView.ItemsLayout>
                <CollectionView.EmptyView>
                    <Label Text="No hay productos disponibles." Style="{StaticResource MutedTextStyle}" />
                </CollectionView.EmptyView>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="m:InventoryItem">
                        <Frame Padding="8" HasShadow="True" Style="{StaticResource CardFrameStyle}">
                            <VerticalStackLayout Spacing="6">
                                <Image Source="{Binding ImageUrl}" HeightRequest="110" Aspect="AspectFill" />
                                <Label Text="{Binding Name}" FontAttributes="Bold" />
                                <Label Text="{Binding Price, StringFormat='{}{0:C}'}" TextColor="{DynamicResource Accent}" />
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- SOBRE NOSOTROS (se mantiene) -->
            <Frame Style="{StaticResource CardFrameStyle}">
                <Grid ColumnDefinitions="*,*" ColumnSpacing="24" RowSpacing="6">
                    <VerticalStackLayout Spacing="8">
                        <Label Text="Sobre Nosotros" Style="{StaticResource SectionTitleStyle}" />
                        <Label Text="Somos una barbería moderna con más de 10 años de experiencia en el sector. Nuestro equipo de profesionales altamente capacitados está comprometido con brindarte el mejor servicio y los cortes más actuales."
                               Style="{StaticResource BodyTextStyle}" />
                        <Label Text="Utilizamos productos de primera calidad y las técnicas más avanzadas para garantizar que salgas luciendo impecable en cada visita."
                               Style="{StaticResource BodyTextStyle}" />
                    </VerticalStackLayout>

                    <VerticalStackLayout Grid.Column="1" Spacing="14">
                        <HorizontalStackLayout Spacing="10">
                            <Border Style="{StaticResource AccentDotStyle}" />
                            <Label Text="Profesionales certificados" Style="{StaticResource BodyTextStyle}" />
                        </HorizontalStackLayout>
                        <HorizontalStackLayout Spacing="10">
                            <Border Style="{StaticResource AccentDotStyle}" />
                            <Label Text="Productos premium" Style="{StaticResource BodyTextStyle}" />
                        </HorizontalStackLayout>
                        <HorizontalStackLayout Spacing="10">
                            <Border Style="{StaticResource AccentDotStyle}" />
                            <Label Text="Ambiente moderno y limpio" Style="{StaticResource BodyTextStyle}" />
                        </HorizontalStackLayout>
                        <HorizontalStackLayout Spacing="10">
                            <Border Style="{StaticResource AccentDotStyle}" />
                            <Label Text="Atención personalizada" Style="{StaticResource BodyTextStyle}" />
                        </HorizontalStackLayout>
                    </VerticalStackLayout>
                </Grid>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
