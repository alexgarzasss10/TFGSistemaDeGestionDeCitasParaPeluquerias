<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SistemasDeGestionCitasPeluqueria.Pages.ReviewDialogPage"
             BackgroundColor="{DynamicResource ModalOverlay}"
             Shell.NavBarIsVisible="False">

    <Grid RowDefinitions="*,Auto,*" ColumnDefinitions="*">
        <!-- Contenedor centrado -->
        <Frame Grid.Row="1"
               Style="{StaticResource CardFrameStyle}"
               HasShadow="True"
               Padding="16"
               Background="{DynamicResource CardBackgroundBrush}">
            <VerticalStackLayout Spacing="14">

                <Grid ColumnDefinitions="*,Auto" VerticalOptions="Center">
                    <Label Text="Escribe tu reseña" Style="{StaticResource SectionTitleStyle}" />
                    <Button Text="✕"
                            BackgroundColor="Transparent"
                            TextColor="{DynamicResource TextSecondary}"
                            Clicked="OnCancelClicked"/>
                </Grid>

                <Label Text="Comparte tu experiencia con nosotros"
                       Style="{StaticResource MutedTextStyle}" />

                <VerticalStackLayout>
                    <Label Text="Valoración" Style="{StaticResource MutedTextStyle}" />
                    <HorizontalStackLayout Spacing="8">
                        <Label x:Name="Star1" FontSize="28" TextColor="{DynamicResource Accent}" Text="☆">
                            <Label.GestureRecognizers><TapGestureRecognizer Tapped="OnStar1Tapped"/></Label.GestureRecognizers>
                        </Label>
                        <Label x:Name="Star2" FontSize="28" TextColor="{DynamicResource Accent}" Text="☆">
                            <Label.GestureRecognizers><TapGestureRecognizer Tapped="OnStar2Tapped"/></Label.GestureRecognizers>
                        </Label>
                        <Label x:Name="Star3" FontSize="28" TextColor="{DynamicResource Accent}" Text="☆">
                            <Label.GestureRecognizers><TapGestureRecognizer Tapped="OnStar3Tapped"/></Label.GestureRecognizers>
                        </Label>
                        <Label x:Name="Star4" FontSize="28" TextColor="{DynamicResource Accent}" Text="☆">
                            <Label.GestureRecognizers><TapGestureRecognizer Tapped="OnStar4Tapped"/></Label.GestureRecognizers>
                        </Label>
                        <Label x:Name="Star5" FontSize="28" TextColor="{DynamicResource Accent}" Text="☆">
                            <Label.GestureRecognizers><TapGestureRecognizer Tapped="OnStar5Tapped"/></Label.GestureRecognizers>
                        </Label>
                    </HorizontalStackLayout>
                </VerticalStackLayout>

                <VerticalStackLayout>
                    <Label Text="Comentario" Style="{StaticResource MutedTextStyle}" />
                    <Editor x:Name="CommentEditor"
                            AutoSize="TextChanges"
                            Placeholder="Cuéntanos tu experiencia..."
                            TextColor="{DynamicResource TextPrimary}"
                            PlaceholderColor="{DynamicResource TextSecondary}"
                            BackgroundColor="Transparent" />
                </VerticalStackLayout>

                <Grid ColumnDefinitions="*,Auto" Margin="0,6,0,0">
                    <Button Text="Cancelar"
                            Padding="18,10"
                            Style="{StaticResource OutlineButtonStyle}"
                            Clicked="OnCancelClicked" />
                    <Button Grid.Column="1"
                            Text="Publicar reseña"
                            Padding="18,10"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Clicked="OnPublishClicked" />
                </Grid>
            </VerticalStackLayout>
        </Frame>
    </Grid>
</ContentPage>