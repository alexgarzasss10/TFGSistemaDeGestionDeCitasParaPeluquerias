<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SistemasDeGestionCitasPeluqueria.Pages.ReviewDialogPage"
             BackgroundColor="{DynamicResource ModalOverlay}"
             Shell.NavBarIsVisible="False">

    <Grid RowDefinitions="*,Auto,*" ColumnDefinitions="*">
        <Frame Grid.Row="1"
               Style="{StaticResource CardFrameStyle}"
               HasShadow="True"
               Padding="16"
               Background="{DynamicResource CardBackgroundBrush}">
            <VerticalStackLayout Spacing="14">

                <Grid ColumnDefinitions="*,Auto" VerticalOptions="Center">
                    <Label Text="Escribe tu reseña" Style="{StaticResource SectionTitleStyle}" />
                    <Button Text="✕"
                            BackgroundColor="Transparent"
                            TextColor="{DynamicResource TextSecondary}"
                            Command="{Binding CancelCommand}"/>
                </Grid>

                <Label Text="Comparte tu experiencia con nosotros"
                       Style="{StaticResource MutedTextStyle}" />

                <VerticalStackLayout>
                    <Label Text="Valoración" Style="{StaticResource MutedTextStyle}" />
                    <HorizontalStackLayout Spacing="8">
                        <Label FontSize="28" TextColor="{DynamicResource Accent}" Text="{Binding Star1Text}">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding SetRatingCommand}" CommandParameter="1"/>
                            </Label.GestureRecognizers>
                        </Label>
                        <Label FontSize="28" TextColor="{DynamicResource Accent}" Text="{Binding Star2Text}">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding SetRatingCommand}" CommandParameter="2"/>
                            </Label.GestureRecognizers>
                        </Label>
                        <Label FontSize="28" TextColor="{DynamicResource Accent}" Text="{Binding Star3Text}">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding SetRatingCommand}" CommandParameter="3"/>
                            </Label.GestureRecognizers>
                        </Label>
                        <Label FontSize="28" TextColor="{DynamicResource Accent}" Text="{Binding Star4Text}">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding SetRatingCommand}" CommandParameter="4"/>
                            </Label.GestureRecognizers>
                        </Label>
                        <Label FontSize="28" TextColor="{DynamicResource Accent}" Text="{Binding Star5Text}">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding SetRatingCommand}" CommandParameter="5"/>
                            </Label.GestureRecognizers>
                        </Label>
                    </HorizontalStackLayout>
                </VerticalStackLayout>

                <VerticalStackLayout>
                    <Label Text="Comentario" Style="{StaticResource MutedTextStyle}" />
                    <Editor AutoSize="TextChanges"
                            Placeholder="Cuéntanos tu experiencia..."
                            Text="{Binding Comment}"
                            TextColor="{DynamicResource TextPrimary}"
                            PlaceholderColor="{DynamicResource TextSecondary}"
                            BackgroundColor="Transparent" />
                </VerticalStackLayout>

                <Grid ColumnDefinitions="*,Auto" Margin="0,6,0,0">
                    <Button Text="Cancelar"
                            Padding="18,10"
                            Style="{StaticResource OutlineButtonStyle}"
                            Command="{Binding CancelCommand}" />
                    <Button Grid.Column="1"
                            Text="Publicar reseña"
                            Padding="18,10"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Command="{Binding PublishCommand}"
                            IsEnabled="{Binding CanPublish}" />
                </Grid>
            </VerticalStackLayout>
        </Frame>
    </Grid>
</ContentPage>